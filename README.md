# testm

roblox dataminer. checks for client updates every 5 minutes, extracts files, diffs them, posts to discord.

tracks locale strings (`CoreScriptLocalization.csv`), corescripts, lua packages, and web js bundles.
